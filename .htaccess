<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /
    
    # SPA routing: Redirect all requests to index.html if the file doesn't exist
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.html [L]
</IfModule>
